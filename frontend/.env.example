# Frontend env template (copy to .env and fill in)

# "demo" for offline play, "live" for real wallet + contract
VITE_MODE=demo

# Backend API URL
VITE_API_URL=http://localhost:3001

# Privy App ID (optional — leave empty to disable)
VITE_PRIVY_APP_ID=
